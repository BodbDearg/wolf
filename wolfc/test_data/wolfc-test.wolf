/; 
This function does an input loop.
This also tests multi line comments.
;/
func inputLoop()
	; Keep asking the user for input
	; If input is <= 0 then we will exit 
	var num = 0

	loop
		print("Hello, enter <= 0 to exit or anything else to see this again! 👻: ")
		num = readnum()
	repeat until num <= 0
end

func main()
	inputLoop()
	print("Wasn't that nice?! 😍\n")
end
